<!--pages/detail-video/index.wxml-->
<!-- autoplay -->
<video class="video" src="{{mvURLInfo.url}}" loop referrer-policy="origin"></video>

<block wx:for="{{relatedVideo}}" wx:key="*this">
  <view>{{item.title}}</view>
</block>